<%= form_for note, url: {action: "comment"}  do |f| %>
   
  <%= f.text_area :comment, size: "60x12" %>
  <%= f.file_field :attachment %>

  <input type="hidden" name="id" value="<%=@goal.id %>" />
  <%= f.submit "Add Note" %>
<% end %>